# Example local env for mise -E local

KUBECONFIG=dev/k3s/kubeconfig/kubeconfig.yaml

# Your phone number (used to populate secret/openclaw-user)
OPENCLAW_OWNER_PHONE=+15551234567

# OpenClaw provider keys (optional, used by local:openclaw:secrets:apply)
# ANTHROPIC_API_KEY=sk-ant-...
# OPENAI_API_KEY=sk-...

# OpenClaw gateway auth (generated automatically if missing)
# Required for OpenClaw when gateway.bind=lan
OPENCLAW_GATEWAY_TOKEN=REPLACE_ME
